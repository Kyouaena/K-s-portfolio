# Source materials

Place offline CV, papers, and DOCX here. Large binaries at repository root are author originals; optionally move them into this folder for cleaner deploys.

The live site reads from `content/*.ts`, not from PDFs directly.
